"""
模型配置文件
调节这些参数来优化预测效果
"""

# ==================== 数据权重配置 ====================

# 跨星级样本权重倍数
# 作用：当训练样本涉及3星→5星转换时，给予更高权重
# 范围：1.0 - 5.0
# 建议：
#   1.0 = 所有样本同等重要
#   2.0 = 跨星级样本权重翻倍（默认）
#   3.0+ = 极度重视跨星级样本（适合垫刀预测场景）
STAR_TRANSITION_WEIGHT = 2.0


# ==================== 滑动窗口预测配置 ====================

# 滑动窗口基础权重（最早样本的权重）
# 范围：0.1 - 0.5
# 建议：0.3（默认）
SLIDING_WINDOW_BASE = 0.3

# 滑动窗口变化范围（最近样本与最早样本的权重差）
# 范围：0.5 - 0.9
# 建议：0.7（默认）
# 最终权重 = BASE + RANGE * (位置比例)
# 例如：0.3 + 0.7 * 1.0 = 1.0（最近样本）
SLIDING_WINDOW_RANGE = 0.7


# ==================== Skip预测配置 ====================

# 最大跳过步数
# 范围：1 - 5
# 建议：3（默认）
# 1 = 只预测下一个
# 2 = 可以跳过1个预测
# 3 = 可以跳过2个预测
# 注意：修改后需要重新训练模型
MAX_SKIP = 3

# Skip=1 权重（近期预测权重）
# 范围：0.5 - 1.0
# 建议：0.6（7词条以上时）或 0.7（5-6词条时）
SKIP_1_WEIGHT_7PLUS = 0.6
SKIP_1_WEIGHT_5TO6 = 0.7

# Skip=2 权重（中期预测权重）
# 范围：0.2 - 0.4
# 建议：0.3
SKIP_2_WEIGHT = 0.3

# Skip=3 权重（长期预测权重）
# 范围：0.05 - 0.2
# 建议：0.1
SKIP_3_WEIGHT = 0.1


# ==================== 模型结构配置 ====================

# 词嵌入维度
# 范围：8 - 32
# 建议：16（默认）
# 注意：修改后需要重新训练模型
EMBED_DIM = 16

# LSTM隐藏层维度
# 范围：32 - 128
# 建议：64（默认）
# 注意：修改后需要重新训练模型
HIDDEN_DIM = 64

# LSTM层数
# 范围：1 - 3
# 建议：2（默认）
# 注意：修改后需要重新训练模型
NUM_LAYERS = 2

# Dropout比率（防止过拟合）
# 范围：0.1 - 0.5
# 建议：0.2（默认）
# 注意：修改后需要重新训练模型
DROPOUT = 0.2

# 输出类别数
# 10 = 只预测类型（默认）
# 20 = 预测星级+类型（需要更多数据）
# 注意：修改后需要重新训练模型
NUM_CLASSES = 10


# ==================== 训练超参数配置 ====================

# 最大训练轮数
# 范围：50 - 200
# 建议：100（默认）
EPOCHS = 100

# 学习率
# 范围：0.0001 - 0.01
# 建议：0.001（默认）
LEARNING_RATE = 0.001

# 早停耐心值（多少轮不提升就停止）
# 范围：5 - 20
# 建议：10（默认）
PATIENCE = 10

# 批次大小
# 范围：16 - 64
# 建议：32（默认）
BATCH_SIZE = 32


# ==================== 置信度计算配置 ====================

# 标准差影响系数（分母）
# 范围：5 - 20
# 建议：10（默认）
# 公式：置信度 = 最大概率 * (1 + 标准差 / 系数)
CONFIDENCE_STD_FACTOR = 10

# 置信度阈值
CONFIDENCE_HIGH = 40    # 高置信度阈值
CONFIDENCE_MEDIUM = 25  # 中置信度阈值


# ==================== 其他配置 ====================

# 训练数据最小要求
MIN_RECORDS_FOR_TRAINING = 10

# 测试集比例
TEST_RATIO = 0.2
